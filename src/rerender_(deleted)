/* import React from 'react';
import ReactDOM from 'react-dom';
import { BrowserRouter } from 'react-router-dom';
import './index.css';
import App from './App';
import { addPost, updateNewPost } from './cheatDataLayer'

export let rerenderTree = (massiveData) => {
  ReactDOM.render(
    <React.StrictMode>
      <BrowserRouter>
        <App data={massiveData} addPost={addPost} updateNewPost={updateNewPost} />
      </BrowserRouter>
    </React.StrictMode>,
    document.getElementById('root')
  );
}
 */
